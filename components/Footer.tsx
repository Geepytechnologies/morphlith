import React from "react";
import Logo from "./Logo";
import Link from "next/link";

type Props = {};

const Footer = (props: Props) => {
  return <div className="h-[250px] bg-secondary"></div>;
};

export default Footer;
